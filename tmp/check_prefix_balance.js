const fs=require('fs');const path='c:\\Users\\Lenovo\\OneDrive\\folder fix\\web\\app\\(dashboard)\\penyetelan-dapur\\page.tsx';const s=fs.readFileSync(path,'utf8');const lines=s.split(/\r?\n/);const upto=804;let pc=0,bc=0,lc=0,rc=0,angOpen=0,angClose=0;for(let i=0;i<upto && i<lines.length;i++){const line=lines[i]; for(const ch of line){ if(ch==='(') pc++; if(ch===')') pc--; if(ch==='{') bc++; if(ch==='}') bc--; if(ch==='[') lc++; if(ch===']') lc--; if(ch==='<' ) angOpen++; if(ch==='>') angClose++; } } console.log('up to line',upto, 'counts: paren',pc,'braces',bc,'brackets',lc,'angleOpen',angOpen,'angleClose',angClose);