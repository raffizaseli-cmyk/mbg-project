const fs=require('fs');const path='c:\\Users\\Lenovo\\OneDrive\\folder fix\\web\\app\\(dashboard)\\penyetelan-dapur\\page.tsx';const s=fs.readFileSync(path,'utf8');let idx=0;let found=0;while(true){idx=s.indexOf('/*', idx); if(idx===-1) break; found++; const start=Math.max(0, idx-40); const end=Math.min(s.length, idx+100); console.log('--- occurrence',found,'at',s.slice(0,idx).split('\n').length, '---'); console.log(s.slice(start,end)); idx=idx+2;} if(found===0) console.log('none');